import React from 'react';

class Muchfit extends React.Component {
    render() {
        let sText = 'Muchfit';

        return (
            <h1>{sText}</h1>
        );
    }
}

export default Muchfit;